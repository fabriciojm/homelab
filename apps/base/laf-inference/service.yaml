apiVersion: v1
kind: Service
metadata:
  name: laf-inference
  namespace: laf
spec:
  ports:
    - name: http
      port: 80
      targetPort: http
  selector:
    app: laf-inference
  type: ClusterIP
