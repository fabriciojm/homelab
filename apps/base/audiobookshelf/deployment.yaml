apiVersion: apps/v1
kind: Deployment
metadata:
  name: audiobookshelf
  namespace: audiobookshelf
spec:
  replicas: 1
  selector:
    matchLabels:
      app: audiobookshelf
  template:
    metadata:
      labels:
        app: audiobookshelf
    spec:
      # securityContext:
      #   fsGroup: 33 # www-data group ID
      #   runAsUser: 33 # www-data user ID
      #   runAsGroup: 33 # www-data group ID

      containers:
        - name: audiobookshelf
          image: ghcr.io/advplyr/audiobookshelf:latest
          #image: sissbruecker/audiobookshelf:1.31.0
          ports:
            - name: http
              containerPort: 80 #metadata
              protocol: TCP

      #     securityContext:
      #       allowPrivilegeEscalation: false

      #     envFrom:
      #       - secretRef:
      #           name: audiobookshelf-container-env

      #     volumeMounts:
      #       - name: audiobookshelf-data
      #         mountPath: /etc/audiobookshelf/data
      # volumes:
      #   - name: audiobookshelf-data
      #     persistentVolumeClaim:
      #       claimName: audiobookshelf-data-pvc
