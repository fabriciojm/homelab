apiVersion: v1
kind: Service
metadata:
  name: audiobookshelf
  namespace: audiobookshelf
spec:
  ports:
    - port: 80
      targetPort: 13378
  selector:
    app: audiobookshelf
  type: ClusterIP
  # type: LoadBalancer
