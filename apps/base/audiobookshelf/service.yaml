apiVersion: v1
kind: Service
metadata:
  name: audiobookshelf
  namespace: audiobookshelf
spec:
  ports:
    - name: http
      # port: 80
      port: 3005
      targetPort: http
      protocol: TCP
  selector:
    app: audiobookshelf
  type: ClusterIP
