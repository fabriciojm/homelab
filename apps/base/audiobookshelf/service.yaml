apiVersion: v1
kind: Service
metadata:
  name: audiobookshelf
  namespace: audiobookshelf
spec:
  ports:
    - name: http
      # port: 3005
      # targetPort: http
      # protocol: TCP
      envFrom:
        - configMapRef:
            name: audiobookshelf-configmap
  selector:
    app: audiobookshelf
  type: ClusterIP
