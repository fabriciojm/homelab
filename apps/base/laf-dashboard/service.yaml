apiVersion: v1
kind: Service
metadata:
  name: laf-dashboard
  namespace: laf
spec:
  ports:
    - name: http
      port: 80
      targetPort: http
  selector:
    app: laf-dashboard
  type: ClusterIP
